spring:
  application:
    name: spring-boot-docker-compose
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://mysqldb:3306/tickets
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
